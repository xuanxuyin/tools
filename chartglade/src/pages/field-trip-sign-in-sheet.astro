---
import ChartPage from '../components/ChartPage.astro';
import SignInSheet from '../components/printables/SignInSheet.astro';
import { signInSheetPages, SHEET_LAYOUTS } from '../data/signInSheets';

const def = signInSheetPages.find((p) => p.slug === 'field-trip-sign-in-sheet')!;
const sheet = SHEET_LAYOUTS['field-trip-sign-in-sheet'];
---

<ChartPage def={def}>
  <SignInSheet
    title="Field Trip Sign-In Sheet"
    metaFields={sheet.metaFields}
    columns={sheet.columns}
    rows={sheet.rows}
    dense={sheet.dense}
  />
</ChartPage>
